# 说明文档

## 1. 仓库定位

本仓库用于沉淀一套可复用的 A 股因子研究工作流，重点不是展示单个“神因子”，而是沉淀一套能够复用、扩展、交接的研究底座。

当前版本主要包含以下几类资产：

- 因子研究 SOP
- 聚宽研究环境基建骨架
- 因子生成逻辑骨架
- 回测策略骨架
- 知识库模板
- 面向管理层的阶段性周报

适合用于：

- 向管理层说明本周沉淀的可复用资产
- 作为后续因子研究项目的起步模板
- 作为团队成员交接和复盘材料

## 2. 仓库目录结构

```text
ai-trading-workflow/
├─ docs/
│  ├─ 个人因子挖掘SOP.md
│  └─ weekly-reports/
│     └─ 2026-08-14_因子挖掘周报_可复用底座_v0.1.docx
├─ knowledge_base/
│  └─ knowledge_base.template.json
├─ src/
│  ├─ jq_research/
│  │  └─ 阶段零_聚宽基建骨架.py
│  ├─ generation/
│  │  └─ 阶段一_生成逻辑骨架.py
│  └─ backtest/
│     └─ 回测环境_策略骨架.py
└─ 说明文档.md
```

## 3. 各文件作用

### docs/个人因子挖掘SOP.md

用于统一研究流程和口径，覆盖候选因子生成、字段/算子校验、短窗口探测、正式回测、结果复盘和入池前检查等步骤。

### docs/weekly-reports/2026-08-14_因子挖掘周报_可复用底座_v0.1.docx

面向 Leader/管理层的阶段性汇报材料，重点说明本周沉淀了哪些可复用能力、当前边界在哪里、下周如何工程化推进。

### src/jq_research/阶段零_聚宽基建骨架.py

研究环境基础骨架，作用是统一数据接入、研究入口和基础设施准备，为后续研究代码提供稳定底座。

### src/generation/阶段一_生成逻辑骨架.py

用于承载候选因子生成、规则拼装、与知识库或 LLM/MCTS 接口对接的生成阶段逻辑。

### src/backtest/回测环境_策略骨架.py

用于承载标准化回测接口、调仓逻辑、绩效评估与因子验证流程，是研究与验证分离的重要骨架文件。

### knowledge_base/knowledge_base.template.json

知识库模板，用于沉淀字段白名单、算子说明、研究约束、提示词结构或因子研究经验，便于后续自动化生成和约束控制。

## 4. 推荐使用方式

建议按照以下顺序阅读和使用：

1. 先看 `docs/个人因子挖掘SOP.md`，理解整体研究流程与质量门禁。
2. 再看 `src/jq_research/阶段零_聚宽基建骨架.py`，了解底层基建入口。
3. 接着看 `src/generation/阶段一_生成逻辑骨架.py`，明确候选因子如何生成和组织。
4. 最后看 `src/backtest/回测环境_策略骨架.py`，理解如何把研究结果接入标准回测。
5. 需要向管理层汇报时，直接查看 `docs/weekly-reports/2026-08-14_因子挖掘周报_可复用底座_v0.1.docx`。

## 5. 本仓库当前边界

当前仓库保存的是“可复用框架与文档资产”，不是一个已经完全产品化的一键运行系统。

现阶段边界包括：

- 部分 LLM / MCTS 接口仍是骨架或模板，并非完整生产实现
- 严格残差正交化能力尚未在当前 CLI 工作流中落地
- 样本外验证、持续监控、自动化报表等能力仍需后续补齐
- 这里不包含比赛账号配置、敏感凭据、缓存、临时状态文件和原始运行产物

## 6. 交付说明

本仓库当前提交的是脱敏后的可复用资产，适合对外做团队内部展示、方案汇报和后续开发接力。

如果后续继续扩展，建议优先补充：

- README
- 环境依赖说明
- 可执行示例
- 回测结果标准输出模板
- 因子提交与复盘台账

## 7. 一句话总结

这不是“某几个因子脚本”的打包，而是一套面向 A 股因子研究的可复用工作流雏形。
